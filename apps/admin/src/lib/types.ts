export enum CouponType {
  PERCENT = 'PERCENT',
  FIXED = 'FIXED',
}

export enum TicketStatus {
  OPEN = 'OPEN',
  REPLIED = 'REPLIED',
  CLOSED = 'CLOSED',
}

export enum ReviewStatus {
  PENDING = 'PENDING',
  APPROVED = 'APPROVED',
}
